/*
    TODO: export to external module
*/
export function sendNotifications(notification: Object) {
    // send the notification to some user
    console.log(`sending the notification to end point ${notification}`)
}
